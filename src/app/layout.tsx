import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Kennedy Emanoel — Desenvolvedor Fullstack",
  description:
    "Kennedy Emanoel, Desenvolvedor Fullstack especializado em soluções ponta a ponta, APIs robustas e arquitetura de software com NestJS, TypeScript, Next.js e PostgreSQL.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
